# ── AI Provider (pick one or both — Ollama takes priority if running) ──────────

# Option 1: Gemini (FREE — 15 req/min, 1M tokens/day)
# Get key at: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=AIzaSy...

# Option 2: Ollama (FREE, runs locally on your Mac)
# Install: https://ollama.com  then run: ollama pull gemma3
# Default URL is http://localhost:11434 — only set if you changed the port
# VITE_OLLAMA_URL=http://localhost:11434
# VITE_OLLAMA_MODEL=gemma3
